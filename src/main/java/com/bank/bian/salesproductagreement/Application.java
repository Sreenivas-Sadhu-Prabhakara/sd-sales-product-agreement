package com.bank.bian.salesproductagreement;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;

/**
 * BIAN Service Domain: Sales Product Agreement
 *
 * Business Area:      Sales and Service
 * Business Domain:    Sales
 * Functional Pattern: Manage
 * Asset Type:         Sales Product Agreement
 * Control Record:     Sales Product Agreement Management Plan
 *
 * Generated by the bian-platform service generator (Phase 1 — shallow build).
 * Replace the in-memory store with real persistence and domain logic in Phase 2.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    @Bean
    public OpenAPI openApi() {
        return new OpenAPI().info(new Info()
                .title("Sales Product Agreement — BIAN Semantic API")
                .description("Service Domain 'Sales Product Agreement' (Manage pattern) "
                        + "operating on the 'Sales Product Agreement Management Plan' control record.")
                .version("0.1.0"));
    }
}
